{
  "name": "mocha-email",
  "version": "1.0.0",
  "description": "Mocha reporter for sending messages via email",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^2.1.0",
    "request": "^2.69.0",
    "sprintf": "^0.1.5"
  }
}
